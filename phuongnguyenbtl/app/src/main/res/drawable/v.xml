<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp" />  <!-- Điều chỉnh giá trị này để thay đổi độ bo tròn -->
    <solid android:color="@android:color/holo_blue_dark" />  <!-- Màu nền cho nút -->
</shape>
